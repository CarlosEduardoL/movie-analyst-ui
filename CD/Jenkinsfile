pipeline {
    agent {
        docker { 
            image 'zeronetdev/rampup-provcli:0.0.1'
            args '-v /home/ubuntu/RampUp/ansible/playbook:/playbook'
        }
    }
    stages {
        stage("deploy"){
            steps {
                sh "echo ${params.TAG}"
                sh "ls /playbook"
            }
        }
    }
}